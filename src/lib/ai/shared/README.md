# Shared AI Context

이 폴더는 메이커 도우미와 플레이어 에이전트가 함께 쓰는
공통 데이터 / 메타데이터 정규화 계층을 둔다.

목적:

- `game` / `session` 원본 포맷이 바뀌어도 AI 기능들이 한 번에 같이 따라가게 하기
- 메이커 AI와 플레이어 AI가 같은 원본에서 출발하되, 서로 다른 공개 범위를 쓰게 하기

원칙:

- 공통 정규화 포맷을 먼저 만든다.
- 메이커 도우미는 전체 또는 검증용 뷰를 쓴다.
- 플레이어 에이전트는 현재 공개된 정보만 담긴 제한 뷰를 쓴다.
